# Copy to .env and fill in. Never commit .env.
EODHD_API_TOKEN=your_token_here

# Guardrails. Keep headroom under the published caps (1000 req/min, 100k calls/day).
XCAP_RATE_PER_MIN=500
XCAP_DAILY_CALL_BUDGET=100000
XCAP_CONCURRENCY=8
